\documentclass[12pt,paper=a4,DIV=15]{ling-handout}

\usepackage{kantlipsum}
\usepackage{minted}

\title{Handout title}

\author{Patrick D.\,Elliott}

\begin{document}

\maketitle

\begin{addmargin}[0pt]{.3\textwidth}

\section{Introduction}

\begin{itemize}

  \item Some sentences. Some sentences. Some sentences. Some sentences. Some sentences. Some sentences.

  \item Some sentences.

  \item Some sentences.\sidenote{Here is a sidenote.}

\ex
This is an example
\xe

\end{itemize}

\section{Math test}

\ex
\(\eval{and\(_{\type{τ → τ → τ}}\)} = \begin{cases}
∧ &\type{τ = t}\\
λ P_{\type{ρ → σ}} . λ Q_{\type{ρ → σ}} . λx_{\type{ρ}} . \eval{and\(_{\type{σ → σ → σ}}\)} (P x) (Q x) & \type{τ = ρ → σ}
\end{cases}\)
\xe

\section{Code test}

\begin{minted}[linenos,tabsize=2,breaklines]{haskell}
newtype CCP a = CCP { (>>-) :: (G Identity a -> G Identity T) -> G Identity T }

instance Functor CCP where
  fmap f cx = CCP $ \k -> cx >>- \x -> k (fmap f x)

instance Applicative CCP where
  pure a = CCP $ \k -> k (pure a)
  cf <*> cx = CCP $ \k -> cf >>- \f -> cx >>- \x -> k (f <*> x)

instance Monad CCP where
  return = pure
  cgx >>= f = CCP $ \k -> cgx >>- \gx -> gx >>= (\x -> f x >>- k)
\end{minted}

\syncwithnotecolumns[paragraphs]

\end{addmargin}

\printbibliography


\end{document}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% TeX-command-extra-options: "-shell-escape"
%%% End:
